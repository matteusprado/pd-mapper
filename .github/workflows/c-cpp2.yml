name: C/C++ CI2

on:
  push:
    branches: [ "support_msm_firmware_loader" ]
  pull_request:
    branches: [ "support_msm_firmware_loader" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: configure
      run: ./configure
    - name: make
      run: make
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
